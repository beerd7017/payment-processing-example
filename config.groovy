environments {
    local {
        instanceProperties = 'local.properties'
    }
    dev {
        instanceProperties = 'dev.properties'
    }
    test {
        instanceProperties = 'test.properties'
    }
    prod {
        instanceProperties = 'production.properties'
    }
}